# PDF-källa — implementationsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Läsa in en föreläsnings-PDF i en kortlek, generera kort ur den, och ställa frågor grundade i just den texten — utan att en föreläsning kostar mer än ett par kronor.

**Architecture:** Texten utvinns med pdf.js i webbläsaren, utan AI och utan serveranrop; PDF-filen lämnar aldrig datorn. Källan lagras i två tabeller — metadata och text var för sig — och båda nås med direkta supabase-anrop, utanför synkens diff. Kortgenerering återanvänder AI-modalen; frågorna använder Anthropics prompt-cachning så att dokumentet bara betalas fullt en gång per timme.

**Tech Stack:** Vanilla JS i ES-moduler, Vite, Vitest, `pdfjs-dist`, Supabase Postgres med radnivåsäkerhet, serverfunktioner i Node på Vercel.

**Spec:** [docs/superpowers/specs/2026-08-30-pdf-kalla-design.md](../specs/2026-08-30-pdf-kalla-design.md)

## Global Constraints

- Gränssnittet är på svenska. Kommentarer förklarar **varför**, aldrig vad.
- Commit-meddelanden på **engelska**, Conventional Commits, ingen attribution-rad. Repot är publikt — se `## Commits` i CLAUDE.md.
- `src/domain/` importerar aldrig från `src/ui/` och testas utan webbläsare.
- Inga hårdkodade färger, radier eller avstånd utanför `src/styles/tokens.css`. Noll `!important`. Aldrig blått. Inga emojis.
- Kontrollhöjd alltid ur `--control-h`. Radie alltid `--r-md` utom listrader (`--r-sm`) och chips (`--r-pill`).
- `api/` har `no-console: error`.
- Appen behöver **ingen** Supabase service role-nyckel.
- Migrationer körs för hand i SQL Editor, i nummerordning, idempotenta.
- Kontraktet i `docs/api-contract.md` ändras **före** implementationen.
- Alla AI-anrop går genom `callAI()` i `src/ai/call.js` och bär ett `feature`.
- Efter varje task: `npm run lint` (0 fel) och `npm test` (allt grönt).

---

### Task 1: Migration 0008 — de två tabellerna

**Files:**
- Create: `supabase/migrations/0008_kallor.sql`

**Interfaces:**
- Consumes: inget.
- Produces: `public.sources` (`id text`, `user_id`, `deck_id`, `title`, `pages`, `chars`, `created_at`, `updated_at`, `deleted_at`) och `public.source_texts` (`source_id text`, `user_id`, `text`).

- [ ] **Step 1: Skriv migrationen**

```sql
-- Repetix — källdokument per kortlek
--
-- Kör detta i Supabase SQL Editor efter 0007. Idempotent.
--
-- Två tabeller, inte en. Texten är hundra kilobyte per föreläsning och behövs
-- bara när AI:n faktiskt ska läsa den; metadatan behövs varje gång kortleken
-- öppnas. Delade de rad hade varje listning dragit med hela texten.
--
-- INGEN av dem går genom synken. Synken är en diff mot S.appData, inte en
-- tabellista: att ta in källor där hade krävt ändringar i appdatan, i
-- flatten(), i diffen och i utkorgen — för en funktion som ändå inte fungerar
-- utan nät, eftersom både generering och frågor går till AI:n. user_settings
-- och ai_usage nås redan på samma sätt, med direkta anrop.

create table if not exists public.sources (
  -- text, inte uuid, och utan default: källan skapas i webbläsaren när texten
  -- utvunnits, precis som en kortlek eller ett kort, och id:t kommer därför ur
  -- nyttId() i klienten. ai_usage kunde ha uuid med default eftersom SERVERN
  -- skapar de raderna.
  id         text primary key,
  user_id    uuid not null references auth.users(id) on delete cascade,
  -- Främmande nyckel med ägarskap, som migration 0005 kräver av varje ny
  -- koppling: en källa ska inte kunna peka på någon annans kortlek.
  deck_id    text not null references public.decks(id) on delete cascade,
  title      text not null,
  pages      integer not null default 0,
  chars      integer not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  -- Mjuk radering, som allt annat användaren äger.
  deleted_at timestamptz
);

create table if not exists public.source_texts (
  source_id text primary key references public.sources(id) on delete cascade,
  user_id   uuid not null references auth.users(id) on delete cascade,
  text      text not null
);

-- Driver den enda frågan: den här kortlekens källor, nyast först.
create index if not exists idx_sources_deck on public.sources (user_id, deck_id, created_at desc);

alter table public.sources enable row level security;
alter table public.source_texts enable row level security;

drop policy if exists own_rows on public.sources;
drop policy if exists own_rows on public.source_texts;

-- Samma form som cards: användaren äger sina rader helt.
create policy own_rows on public.sources
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy own_rows on public.source_texts
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

-- updated_at-triggern som resten av tabellerna har.
drop trigger if exists set_updated_at on public.sources;
create trigger set_updated_at before update on public.sources
  for each row execute function public.set_updated_at();
```

- [ ] **Step 2: Kör den i Supabase SQL Editor**

Kör hela filen, sedan **en gång till** — varje sats är idempotent och andra körningen ska gå igenom utan fel.

- [ ] **Step 3: Kontrollera policys**

```sql
select tablename, policyname, cmd from pg_policies
 where tablename in ('sources','source_texts');
```

Förväntat: två rader, båda `own_rows` med `cmd = ALL`.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0008_kallor.sql
git commit -m "feat(db): two tables for a deck's source documents

Metadata and text are separated because the text is a hundred kilobytes
per lecture and is only needed when the model actually reads it, while
the metadata is needed every time the deck is opened.

Neither goes through the sync. The sync is a diff against S.appData
rather than a list of tables, so taking sources into it would have meant
changing the app data, flatten(), the diff and the outbox — for a feature
that does not work offline anyway."
```

---

### Task 2: Kontraktet — `cache` och `effort`

**Files:**
- Modify: `docs/api-contract.md`

**Interfaces:**
- Consumes: inget.
- Produces: kontraktets beskrivning av `cache` och `effort` i `POST /api/ai`. Task 7 och 8 implementerar det.

- [ ] **Step 1: Lägg till fälten i begäran under `## POST /api/ai`**

Lägg in efter raden som beskriver `feature`:

````markdown
Två valfria fält styr kostnaden. **Bara Anthropic-adaptern läser dem**; de
övriga tre ignorerar dem tyst, så en klient behöver inte veta vilken leverantör
användaren valt.

```jsonc
{
  // Cachar systemprompten hos leverantören i en timme. Sätts när samma långa
  // text ska frågas om flera gånger — en cachad läsning kostar en tiondel.
  "cache": true,

  // Skär ned modellens tänkande, som debiteras som utdata. Enda tillåtna
  // värdet är "low". Skickas ALDRIG till claude-haiku-4-5, som avvisar
  // parametern; servern utelämnar den då själv.
  "effort": "low"
}
```
````

- [ ] **Step 2: Commit**

```bash
git add docs/api-contract.md
git commit -m "docs(api): add the cache and effort fields

The contract changes before the implementation does, per CLAUDE.md.

Both exist to control cost and both are Anthropic-only, so they are
documented as optional hints the other adapters ignore rather than as
guarantees the caller can rely on."
```

---

### Task 3: Textutvinning

**Files:**
- Create: `src/domain/kalltext.js`
- Create: `src/core/pdf.js`
- Test: `tests/kalltext.test.js`
- Modify: `package.json` (lägg till `pdfjs-dist`)

**Interfaces:**
- Consumes: inget.
- Produces: `TECKENTAK` (number), `sammanfogaSidor(sidor: string[]) → string`, `overTaket(text: string) → boolean` från `src/domain/kalltext.js`; `laesPdf(file: File) → Promise<{text: string, pages: number, chars: number}>` från `src/core/pdf.js`.

- [ ] **Step 1: Installera pdf.js**

```bash
npm install pdfjs-dist
```

- [ ] **Step 2: Skriv de fallerande testerna**

Skapa `tests/kalltext.test.js`:

```js
import { describe, expect, it } from 'vitest';
import { TECKENTAK, overTaket, sammanfogaSidor } from '../src/domain/kalltext.js';

describe('sammanfogaSidor', () => {
  it('fogar ihop sidor med blankrad emellan', () => {
    expect(sammanfogaSidor(['sida ett', 'sida två'])).toBe('sida ett\n\nsida två');
  });

  /* pdf.js ger ofta rader med efterhängande blanksteg och tomma sidor för
   * omslag och avdelare. En tom sida ska inte bli en blankrad till. */
  it('hoppar över tomma sidor och trimmar', () => {
    expect(sammanfogaSidor(['  ett  ', '   ', '', 'två'])).toBe('ett\n\ntvå');
  });

  it('ger tom sträng när ingenting gick att utvinna', () => {
    expect(sammanfogaSidor([])).toBe('');
    expect(sammanfogaSidor(['', '   '])).toBe('');
  });
});

describe('overTaket', () => {
  /* Taket finns för att en hel kursbok annars spränger både kontexten och
   * plånboken. Gränsen prövas exakt, inte ungefär: ett tak som råkar vara ett
   * tecken fel är ett tak ingen kan resonera om. */
  it('är falskt precis på gränsen och sant ett tecken över', () => {
    expect(overTaket('x'.repeat(TECKENTAK))).toBe(false);
    expect(overTaket('x'.repeat(TECKENTAK + 1))).toBe(true);
  });

  it('klarar tom text', () => {
    expect(overTaket('')).toBe(false);
  });
});
```

- [ ] **Step 3: Kör och se att det fallerar**

```bash
npx vitest run tests/kalltext.test.js
```

Förväntat: FAIL, modulen finns inte.

- [ ] **Step 4: Skriv domänmodulen**

Skapa `src/domain/kalltext.js`:

```js
/* Källtexten som den ser ut efter utvinning ur en PDF.
 *
 * Ren funktion utan DOM: pdf.js ger en lista med sidor, och det som är värt att
 * pröva är vad som händer med dem — inte att biblioteket fungerar.
 */

/* Taket i tecken, ungefär 50 000 tokens.
 *
 * En hel kursbok spränger både kontextfönstret och plånboken. Gränsen sitter
 * här och inte i gränssnittet, så att den gäller oavsett vem som anropar. */
export const TECKENTAK = 200_000;

/**
 * Fogar ihop pdf.js sidor till en text.
 *
 * Tomma sidor hoppas över: omslag och avdelare ger tomma strängar, och en
 * blankrad per sådan hade gett en text som mest består av luft.
 *
 * @param {string[]} sidor
 * @returns {string}
 */
export function sammanfogaSidor(sidor) {
  return (sidor ?? [])
    .map((s) => String(s ?? '').trim())
    .filter(Boolean)
    .join('\n\n');
}

/** Är texten för lång för att skickas? */
export function overTaket(text) {
  return String(text ?? '').length > TECKENTAK;
}
```

- [ ] **Step 5: Kör testet**

```bash
npx vitest run tests/kalltext.test.js
```

Förväntat: PASS.

- [ ] **Step 6: Skriv pdf.js-lagret**

Skapa `src/core/pdf.js`:

```js
/* Textutvinning ur en PDF, i webbläsaren.
 *
 * Ingen AI och inget serveranrop: ett LaTeX-satt PDF har ett riktigt textlager,
 * och att läsa det kostar ingenting. Filen lämnar aldrig datorn — bara den
 * utvunna texten skickas vidare, och först när användaren ber om något.
 *
 * MATEMATIKEN KOMMER UT TRASIG. Formler sätts som enskilda glyfer med egna
 * teckenkodningar, och det finns ingen väg runt det här. Därför visas texten
 * för användaren innan något AI-anrop görs.
 */

import { sammanfogaSidor } from '../domain/kalltext.js';

/* pdf.js läser i en worker. Utan den här raden kastar biblioteket vid första
 * dokumentet — och felet ("No GlobalWorkerOptions.workerSrc specified") pekar
 * inte på att det är Vite-uppsättningen som saknas. ?url ger Vite:s hashade
 * sökväg i stället för en väg som bara råkar stämma i utveckling. */
let pdfjs = null;

async function ladda() {
  if (pdfjs) return pdfjs;
  const [lib, worker] = await Promise.all([
    import('pdfjs-dist'),
    import('pdfjs-dist/build/pdf.worker.min.mjs?url'),
  ]);
  lib.GlobalWorkerOptions.workerSrc = worker.default;
  pdfjs = lib;
  return pdfjs;
}

/**
 * Läser en PDF och ger dess text.
 *
 * @param {File} file
 * @returns {Promise<{text: string, pages: number, chars: number}>}
 */
export async function laesPdf(file) {
  const lib = await ladda();
  const buffer = await file.arrayBuffer();
  const doc = await lib.getDocument({ data: buffer }).promise;

  const sidor = [];
  for (let n = 1; n <= doc.numPages; n++) {
    const page = await doc.getPage(n);
    const content = await page.getTextContent();
    sidor.push(content.items.map((post) => post.str ?? '').join(' '));
  }

  const text = sammanfogaSidor(sidor);
  return { text, pages: doc.numPages, chars: text.length };
}
```

- [ ] **Step 7: Kör allt**

```bash
npm run lint && npm test && npm run build
```

Bygget ska gå igenom — det är där en felaktig worker-import syns.

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json src/domain/kalltext.js src/core/pdf.js tests/kalltext.test.js
git commit -m "feat(kalla): read the text out of a PDF in the browser

No AI and no server call: a LaTeX-set PDF has a real text layer and
reading it costs nothing. The file never leaves the machine — only the
extracted text is sent, and only when the user asks for something.

The maths comes out broken. Formulas are set as individual glyphs with
their own encodings and there is no way around that here, which is why
the text is shown before any model call is made.

pdf.js reads in a worker; without the workerSrc line it throws on the
first document with an error that does not point at the build setup."
```

---

### Task 4: Lagring av källor

**Files:**
- Create: `src/core/sources.js`
- Test: `tests/sources.test.js`

**Interfaces:**
- Consumes: `nyttId()` från `src/core/utils.js`.
- Produces: `sparaKalla({deckId, title, text, pages}) → Promise<{ok: boolean, source?: object, error?: string}>`, `hamtaKallor(deckId) → Promise<object[]>`, `hamtaKalltext(sourceId) → Promise<string|null>`, `taBortKalla(sourceId) → Promise<{ok: boolean}>` från `src/core/sources.js`.

- [ ] **Step 1: Skriv de fallerande testerna**

Skapa `tests/sources.test.js`:

```js
import { describe, expect, it, vi } from 'vitest';

const session = { value: { user: { id: 'u1' } } };
vi.mock('../src/core/supabase.js', () => ({
  supabase: globalThis.__falskSupabase,
  getUserId: () => session.value?.user?.id ?? null,
}));

const { sparaKalla } = await import('../src/core/sources.js');

/* Två tabeller skrivs i följd, och den andra kan misslyckas efter att den
 * första lyckats. Utan städning blir raden i sources en källa utan text —
 * synlig i listan, omöjlig att använda. */
describe('sparaKalla', () => {
  it('skriver metadata och text, och ger tillbaka källan', async () => {
    const skrivet = [];
    globalThis.__falskSupabase.__svar = { error: null };
    globalThis.__falskSupabase.__logg = skrivet;

    const res = await sparaKalla({ deckId: 'd1', title: 'F1', text: 'hej', pages: 3 });

    expect(res.ok).toBe(true);
    expect(skrivet[0].tabell).toBe('sources');
    expect(skrivet[0].rad).toMatchObject({ user_id: 'u1', deck_id: 'd1', title: 'F1', pages: 3, chars: 3 });
    expect(skrivet[1].tabell).toBe('source_texts');
    expect(skrivet[1].rad).toMatchObject({ user_id: 'u1', text: 'hej' });
    expect(skrivet[1].rad.source_id).toBe(skrivet[0].rad.id);
  });

  it('städar bort metadataraden när texten inte gick att spara', async () => {
    const skrivet = [];
    globalThis.__falskSupabase.__logg = skrivet;
    globalThis.__falskSupabase.__svarPerTabell = {
      sources: { error: null },
      source_texts: { error: { message: 'nekad' } },
    };

    const res = await sparaKalla({ deckId: 'd1', title: 'F1', text: 'hej', pages: 1 });

    expect(res.ok).toBe(false);
    expect(skrivet.some((s) => s.tabell === 'sources' && s.op === 'delete')).toBe(true);
  });
});
```

Lägg den falska klienten högst upp i filen, före `vi.mock`:

```js
globalThis.__falskSupabase = {
  __logg: [],
  __svar: { error: null },
  __svarPerTabell: null,
  from(tabell) {
    const self = this;
    const svar = () => self.__svarPerTabell?.[tabell] ?? self.__svar;
    return {
      insert: async (rad) => {
        self.__logg.push({ tabell, op: 'insert', rad });
        return svar();
      },
      delete() {
        return {
          eq: async () => {
            self.__logg.push({ tabell, op: 'delete' });
            return { error: null };
          },
        };
      },
    };
  },
};
```

- [ ] **Step 2: Kör och se att det fallerar**

```bash
npx vitest run tests/sources.test.js
```

Förväntat: FAIL, modulen finns inte.

- [ ] **Step 3: Skriv modulen**

Skapa `src/core/sources.js`:

```js
/* Källdokument per kortlek.
 *
 * Direkta anrop mot supabase, utanför synken — som user_settings och ai_usage.
 * Synken är en diff mot S.appData, inte en tabellista, och att ta in källor där
 * hade krävt ändringar i appdatan, i flatten(), i diffen och i utkorgen. För en
 * funktion som ändå kräver nät är det fel pris.
 *
 * Metadata och text ligger i var sin tabell. Texten är hundra kilobyte per
 * föreläsning och hämtas bara när den faktiskt ska läsas.
 */

import { getUserId, supabase } from './supabase.js';
import { nyttId } from './utils.js';

/**
 * Sparar en inläst källa.
 *
 * Två skrivningar i följd, och den andra kan misslyckas efter att den första
 * lyckats. Då städas den första bort: en rad i sources utan rad i source_texts
 * är en källa som syns i listan men inte går att använda, och den sortens
 * halvtillstånd är värre än ett fel användaren ser direkt.
 */
export async function sparaKalla({ deckId, title, text, pages }) {
  const userId = getUserId();
  if (!supabase || !userId) return { ok: false, error: 'Du är inte inloggad.' };

  const id = nyttId();
  const rad = {
    id,
    user_id: userId,
    deck_id: deckId,
    title,
    pages: pages ?? 0,
    chars: text.length,
  };

  const { error: metafel } = await supabase.from('sources').insert(rad);
  if (metafel) return { ok: false, error: 'Kunde inte spara källan.' };

  const { error: textfel } = await supabase
    .from('source_texts')
    .insert({ source_id: id, user_id: userId, text });

  if (textfel) {
    await supabase.from('sources').delete().eq('id', id);
    return { ok: false, error: 'Kunde inte spara källans text.' };
  }

  return { ok: true, source: rad };
}

/** Kortlekens källor, nyast först. Utan texten. */
export async function hamtaKallor(deckId) {
  const userId = getUserId();
  if (!supabase || !userId) return [];

  const { data, error } = await supabase
    .from('sources')
    .select('id, title, pages, chars, created_at')
    .eq('deck_id', deckId)
    .is('deleted_at', null)
    .order('created_at', { ascending: false });

  return error ? [] : (data ?? []);
}

/** Texten till en källa. Hämtas först när den ska läsas. */
export async function hamtaKalltext(sourceId) {
  if (!supabase) return null;
  const { data, error } = await supabase
    .from('source_texts')
    .select('text')
    .eq('source_id', sourceId)
    .maybeSingle();

  return error ? null : (data?.text ?? null);
}

/** Mjuk radering, som allt annat användaren äger. */
export async function taBortKalla(sourceId) {
  if (!supabase) return { ok: false };
  const { error } = await supabase
    .from('sources')
    .update({ deleted_at: new Date().toISOString() })
    .eq('id', sourceId);
  return { ok: !error };
}
```

- [ ] **Step 4: Kör testet och allt annat**

```bash
npx vitest run tests/sources.test.js && npm run lint && npm test
```

- [ ] **Step 5: Commit**

```bash
git add src/core/sources.js tests/sources.test.js
git commit -m "feat(kalla): store a deck's sources outside the sync

Direct supabase calls, the way user_settings and ai_usage already work.
The sync is a diff against S.appData rather than a list of tables, so
taking sources into it would have meant changing the app data, flatten(),
the diff and the outbox — the wrong price for a feature that needs the
network anyway.

Two writes in sequence, and the second can fail after the first
succeeded. The metadata row is then removed: a source with no text shows
up in the list and cannot be used, and that half-state is worse than an
error the user sees immediately."
```

---

### Task 5: Källan i kortleksvyn

**Files:**
- Modify: `index.html` (knapp i åtgärdsraden, källrad, dold filinmatning)
- Modify: `src/ui/deck.js` (rendera källor)
- Create: `src/ui/wiring/kalla.js`
- Modify: `src/main.js` (koppla in)
- Modify: `src/styles/views/deck.css`

**Interfaces:**
- Consumes: `laesPdf` (Task 3); `sparaKalla`, `hamtaKallor`, `taBortKalla` (Task 4); `TECKENTAK`, `overTaket` (Task 3).
- Produces: `initUiWiringKalla()` från `src/ui/wiring/kalla.js`; `renderaKallor(deckId)` från `src/ui/deck.js`.

- [ ] **Step 1: Lägg markup i åtgärdsraden**

I `index.html`, efter `<button id="btn-ai-sort" class="btn">AI-sortera</button>`:

```html
                <button id="btn-lagg-till-kalla" class="btn">Lägg till PDF</button>
                <!-- Filinmatningen är dold och triggas av knappen: en naken
                     <input type="file"> går inte att forma efter appens
                     kontroller, och accept begränsar väljaren till PDF. -->
                <input type="file" id="input-kalla-fil" accept="application/pdf,.pdf" hidden>
```

Och direkt efter `</div>` som stänger åtgärdsraden, före `<div id="deck-ai-insights"`:

```html
            <!-- Kortlekens källor. Tom lista döljer hela raden — en rubrik över
                 ingenting är brus. -->
            <ul id="deck-kallor" class="deck-kallor" hidden></ul>
```

- [ ] **Step 2: Rendera källorna**

I `src/ui/deck.js`, lägg importerna:

```js
import { hamtaKallor, taBortKalla } from '../core/sources.js';
```

Och funktionen, exporterad:

```js
/* Källorna hämtas vid varje öppning i stället för att cachas i S: de ändras
 * sällan, listan är kort, och en cache som blir gammal visar en källa som
 * raderats på en annan enhet. */
export async function renderaKallor(deckId) {
  const lista = document.getElementById('deck-kallor');
  if (!lista) return;

  const kallor = await hamtaKallor(deckId);
  lista.hidden = kallor.length === 0;
  lista.innerHTML = '';

  for (const kalla of kallor) {
    const li = document.createElement('li');
    li.className = 'deck-kalla';
    li.dataset.kalla = kalla.id;

    const namn = document.createElement('span');
    namn.className = 'deck-kalla-namn';
    namn.textContent = kalla.title;

    const meta = document.createElement('span');
    meta.className = 'deck-kalla-meta num';
    meta.textContent = `${kalla.pages} sidor · ${kalla.chars.toLocaleString('sv-SE')} tecken`;

    const generera = document.createElement('button');
    generera.type = 'button';
    generera.className = 'btn';
    generera.dataset.kallaHandling = 'generera';
    generera.textContent = 'Generera kort';

    const fraga = document.createElement('button');
    fraga.type = 'button';
    fraga.className = 'btn';
    fraga.dataset.kallaHandling = 'fraga';
    fraga.textContent = 'Ställ en fråga';

    const bort = document.createElement('button');
    bort.type = 'button';
    bort.className = 'btn text';
    bort.dataset.kallaHandling = 'bort';
    bort.textContent = 'Ta bort';
    bort.addEventListener('click', async () => {
      await taBortKalla(kalla.id);
      void renderaKallor(deckId);
    });

    li.append(namn, meta, generera, fraga, bort);
    lista.appendChild(li);
  }
}
```

Anropa den sist i `openDeck`, efter `applyWash(heroStatus, S.currentDeckId);`:

```js
    void renderaKallor(id);
```

- [ ] **Step 3: Koppla inläsningen**

Skapa `src/ui/wiring/kalla.js`:

```js
/* Inläsningen av en PDF.
 *
 * Två steg med flit: extraktionen är gratis och tar en sekund, och att se att
 * texten blev läsbar innan man betalar för ett AI-anrop spelar roll just här —
 * LaTeX-matematik kommer ut trasig, och fyrtio kort byggda på grus är dyrare
 * att rensa bort än att aldrig ha beställt.
 */

import { S } from '../../core/state.js';
import { laesPdf } from '../../core/pdf.js';
import { sparaKalla } from '../../core/sources.js';
import { TECKENTAK, overTaket } from '../../domain/kalltext.js';
import { renderaKallor } from '../deck.js';
import { showToast } from '../toast.js';

export function initUiWiringKalla() {
  const knapp = document.getElementById('btn-lagg-till-kalla');
  const falt = document.getElementById('input-kalla-fil');
  if (!knapp || !falt) return;

  knapp.addEventListener('click', () => falt.click());

  falt.addEventListener('change', async () => {
    const fil = falt.files?.[0];
    // Fältet nollställs direkt: väljs samma fil igen utlöses annars ingen
    // change-händelse, och knappen ser trasig ut.
    falt.value = '';
    if (!fil) return;

    knapp.disabled = true;
    knapp.textContent = 'Läser...';

    try {
      const { text, pages, chars } = await laesPdf(fil);

      if (!text) {
        showToast('Ingen text gick att läsa ur PDF:en. Är den inskannad?');
        return;
      }
      if (overTaket(text)) {
        showToast(
          `Texten är ${chars.toLocaleString('sv-SE')} tecken. Taket är ${TECKENTAK.toLocaleString('sv-SE')}.`
        );
        return;
      }

      const res = await sparaKalla({
        deckId: S.currentDeckId,
        title: fil.name.replace(/\.pdf$/i, ''),
        text,
        pages,
      });

      if (!res.ok) return showToast(res.error);
      showToast(`${pages} sidor inlästa.`);
      void renderaKallor(S.currentDeckId);
    } catch {
      showToast('Kunde inte läsa PDF:en.');
    } finally {
      knapp.disabled = false;
      knapp.textContent = 'Lägg till PDF';
    }
  });
}
```

- [ ] **Step 4: Koppla in i main.js**

Lägg importen bland de andra `wiring`-importerna och anropet efter `initUiWiringAddCard();`:

```js
import { initUiWiringKalla } from './ui/wiring/kalla.js';
```

```js
  initUiWiringKalla();
```

- [ ] **Step 5: Stil**

Lägg sist i `src/styles/views/deck.css`:

```css
/* Källorna står som rader, inte kort: de är en lista över vad kortleken vilar
   på, och ett kort per föreläsning hade tävlat med korten nedanför. */
#view-deck .deck-kallor {
  display: flex;
  flex-direction: column;
  gap: var(--s2);
  margin-bottom: var(--s5);
  padding: 0;
  list-style: none;
}

#view-deck .deck-kalla {
  display: flex;
  align-items: center;
  gap: var(--s3);
  padding: var(--s2) var(--s3);
  background: var(--surface-1);
  border: 1px solid var(--line);
  /* Listrad, inte knapp — därför den mindre radien, enligt tokens.css. */
  border-radius: var(--r-sm);
}

#view-deck .deck-kalla-namn {
  flex: 1 1 auto;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-weight: 600;
}

#view-deck .deck-kalla-meta {
  flex: 0 0 auto;
  font-size: var(--t-xs);
  color: var(--text-3);
}

@media (max-width: 600px) {
  /* På telefon får raden två våningar hellre än fem hopklämda kolumner. */
  #view-deck .deck-kalla {
    flex-wrap: wrap;
  }

  #view-deck .deck-kalla-namn {
    flex: 1 1 100%;
  }
}
```

- [ ] **Step 6: Verifiera i webbläsaren**

`preview_start` med namnet `repetix`. Logga in, öppna en kortlek, lägg till en PDF. Kontrollera: raden dyker upp med sidantal och teckenantal, "Ta bort" tar bort den, och listan är dold när inga källor finns.

- [ ] **Step 7: Commit**

```bash
git add index.html src/ui/deck.js src/ui/wiring/kalla.js src/main.js src/styles/views/deck.css
git commit -m "feat(kortlek): add a PDF to a deck and see what came out

Two steps on purpose. Extraction is free and takes a second, and seeing
that the text came out readable before paying for a model call matters
here specifically: LaTeX maths comes out broken, and forty cards built on
gravel cost more to clear than never ordering them.

The file input is hidden behind a real button — a bare input cannot be
shaped like the app's other controls."
```

---

### Task 6: Kort ur källan

**Files:**
- Modify: `index.html` (tredje segmentet i källväljaren)
- Modify: `src/ai/wiring/topic-generator.js`
- Modify: `src/ai/topic-generator.js`
- Modify: `src/ui/deck.js` (knappen öppnar modalen med källan vald)

**Interfaces:**
- Consumes: `hamtaKalltext` (Task 4).
- Produces: `S.aiGeneratorOptions.sourceType === 'kalla'` och `S.aiGeneratorOptions.sourceId`.

- [ ] **Step 1: Lägg det tredje segmentet**

I `index.html`, i `.toggle-button-group` inuti `#modal-topic-generator`, efter `toggle-source-text`:

```html
                        <button type="button" id="toggle-source-kalla" class="btn-toggle" hidden>Från PDF</button>
```

Och efter `#text-input-container`:

```html
                <div id="kalla-input-container" class="form-group hidden">
                    <label>Källa</label>
                    <p id="kalla-vald-namn" class="form-hint"></p>
                </div>
```

Segmentet är `hidden` som förval och visas bara när modalen öppnats från en källa — ett val som inte går att göra ska inte stå framme.

- [ ] **Step 2: Koppla segmentet**

I `src/ai/wiring/topic-generator.js`, bredvid de två befintliga hanterarna:

```js
      document.getElementById('toggle-source-kalla').addEventListener('click', () => {
          document.getElementById('toggle-source-kalla').classList.add('active');
          document.getElementById('toggle-source-topic').classList.remove('active');
          document.getElementById('toggle-source-text').classList.remove('active');
          document.getElementById('kalla-input-container').classList.remove('hidden');
          document.getElementById('topic-input-container').classList.add('hidden');
          document.getElementById('text-input-container').classList.add('hidden');
          S.aiGeneratorOptions.sourceType = 'kalla';
      });
```

Och i återställningsblocket som körs när modalen öppnas (i dag rad 39–52, där
`toggle-source-topic` får `active`), lägg till sist i blocket:

```js
          /* Källsegmentet göms alltid vid öppning och visas bara av den som
           * öppnar modalen FRÅN en källa. Ordningen spelar roll: raden ovanför
           * skriver om hela S.aiGeneratorOptions, så ett sourceId satt före
           * klicket hade försvunnit här. Därför sätts det efteråt i stället. */
          document.getElementById('toggle-source-kalla').classList.remove('active');
          document.getElementById('toggle-source-kalla').hidden = true;
          document.getElementById('kalla-input-container').classList.add('hidden');
          S.aiGeneratorOptions.sourceId = null;
```

- [ ] **Step 3: Hämta texten vid generering**

I `src/ai/topic-generator.js`, lägg importen:

```js
import { hamtaKalltext } from '../core/sources.js';
```

Och i `fetchCardsByTopic`, före `let sourceInstruction = "";`:

```js
    /* Källans text hämtas först här, inte när källan valdes: den är hundra
     * kilobyte och behövs bara i det ögonblick den ska skickas. */
    let kallText = null;
    if (S.aiGeneratorOptions.sourceType === 'kalla') {
        kallText = await hamtaKalltext(S.aiGeneratorOptions.sourceId);
        if (!kallText) {
            showToast('Kunde inte läsa källans text.');
            return;
        }
    }
```

Utöka sedan `sourceInstruction`:

```js
    if (S.aiGeneratorOptions.sourceType === 'kalla') {
        sourceInstruction = `Du MÅSTE utgå strikt ifrån följande föreläsningstext som källmaterial. Hämta all information och fakta från denna text:\n\n"""\n${kallText}\n"""`;
    } else if (S.aiGeneratorOptions.sourceType === 'text') {
```

Och byt `feature` i `callAIDetailed`-anropet:

```js
            feature: S.aiGeneratorOptions.sourceType === 'kalla' ? 'kalla-kort' : 'topic',
```

- [ ] **Step 4: Öppna modalen från källraden**

I `src/ui/deck.js`, i `renderaKallor`, koppla knappen:

Ordningen är inte godtycklig: öppningsknappen skriver om hela
`S.aiGeneratorOptions`, så källan måste sättas **efter** klicket.

```js
    generera.addEventListener('click', () => {
      // 1. Öppna modalen. Detta nollställer S.aiGeneratorOptions helt.
      document.getElementById('btn-open-topic-generator').click();

      // 2. Först nu går det att peka ut källan — ett sourceId satt före klicket
      //    hade skrivits över av återställningen.
      S.aiGeneratorOptions.sourceId = kalla.id;

      // 3. Visa segmentet och välj det. Återställningen gömde det, eftersom den
      //    inte kan veta att modalen öppnades från en källa.
      const segment = document.getElementById('toggle-source-kalla');
      segment.hidden = false;
      segment.click();
      document.getElementById('kalla-vald-namn').textContent = kalla.title;
    });
```

- [ ] **Step 5: Kör allt och verifiera i webbläsaren**

```bash
npm run lint && npm test && npm run build
```

Öppna en kortlek med en källa, tryck **Generera kort**, kontrollera att modalen öppnas med källsegmentet valt och källans namn utskrivet, och att korten som genereras handlar om föreläsningen.

- [ ] **Step 6: Commit**

```bash
git add index.html src/ai/wiring/topic-generator.js src/ai/topic-generator.js src/ui/deck.js
git commit -m "feat(kalla): generate cards from a source

The AI modal's source selector already had two segments; the source
becomes a third rather than a second modal. Count, difficulty, focus and
target folder work exactly as before.

The segment is hidden unless the modal was opened from a source — a
choice that cannot be made should not be on display. The text itself is
fetched at generation time, not when the source is picked: it is a
hundred kilobytes and is only needed the moment it is sent."
```

---

### Task 7: `effort` genom AI-lagret

**Files:**
- Modify: `api/_lib/providers.js` (Anthropic-adaptern)
- Modify: `api/ai.js` (läs fältet)
- Modify: `src/ai/call.js` (skicka fältet)
- Modify: `src/ai/topic-generator.js` (sätt det vid källgenerering)
- Test: `tests/providers.test.js`

**Interfaces:**
- Consumes: kontraktet från Task 2.
- Produces: `callAI({..., effort})`; `buildRequest({..., effort})` som lägger `output_config.effort` i kroppen för Anthropic-modeller som stödjer det.

- [ ] **Step 1: Skriv de fallerande testerna**

Lägg till sist i `tests/providers.test.js`:

```js
/* effort skär ned tänkandet, som debiteras som utdata — den enda leveran utöver
 * modellvalet. Men parametern avvisas av claude-haiku-4-5, så adaptern måste
 * utelämna den där i stället för att låta anropet gå sönder. */
describe('effort', () => {
  it('lägger output_config.effort i kroppen för Opus', () => {
    const { body } = providers.anthropic.buildRequest(
      anrop({ model: 'claude-opus-5', effort: 'low' })
    );
    expect(body.output_config).toEqual({ effort: 'low' });
  });

  it('utelämnar fältet för haiku, som avvisar det', () => {
    const { body } = providers.anthropic.buildRequest(
      anrop({ model: 'claude-haiku-4-5', effort: 'low' })
    );
    expect(body.output_config).toBeUndefined();
  });

  it('utelämnar fältet när ingen effort begärts', () => {
    const { body } = providers.anthropic.buildRequest(anrop({ model: 'claude-opus-5' }));
    expect(body.output_config).toBeUndefined();
  });

  /* De andra tre leverantörerna ska ignorera fältet tyst — klienten ska inte
   * behöva veta vilken leverantör användaren valt. */
  it('rör inte de andra adaptrarnas kroppar', () => {
    for (const id of ['openai', 'google', 'openrouter']) {
      const { body } = providers[id].buildRequest(anrop({ effort: 'low' }));
      expect(body.output_config).toBeUndefined();
    }
  });
});
```

- [ ] **Step 2: Kör och se att det fallerar**

```bash
npx vitest run tests/providers.test.js
```

Förväntat: FAIL, `body.output_config` är `undefined` i det första testet.

- [ ] **Step 3: Implementera i adaptern**

I `api/_lib/providers.js`, ovanför `export const providers`:

```js
/* Modeller som avvisar output_config.effort.
 *
 * Haiku 4.5 svarar med fel om fältet skickas. Listan är en nekandelista och
 * inte en tillåtandelista med flit: leverantörerna släpper nya modeller oftare
 * än appen uppdateras, och en ny modell ska få effort som förval snarare än att
 * tyst tappa besparingen.
 */
const UTAN_EFFORT = new Set(['claude-haiku-4-5']);
```

Och i Anthropic-adapterns `buildRequest`, ändra signaturen och kroppen:

```js
    buildRequest({ system, user, maxTokens, model, json, key, effort }) {
      const prompt = withJsonInstruction(system, json);
      return {
        url: 'https://api.anthropic.com/v1/messages',
        headers: {
          'content-type': 'application/json',
          'x-api-key': key,
          'anthropic-version': ANTHROPIC_VERSION,
        },
        body: {
          model,
          max_tokens: maxTokens,
          ...(prompt ? { system: prompt } : {}),
          ...(effort && !UTAN_EFFORT.has(model) ? { output_config: { effort } } : {}),
          messages: [{ role: 'user', content: user }],
        },
      };
    },
```

- [ ] **Step 4: Kör testet**

```bash
npx vitest run tests/providers.test.js
```

Förväntat: PASS.

- [ ] **Step 5: Låt fältet ta sig hela vägen**

I `api/ai.js`, bland fältgränserna:

```js
/* Enda tillåtna värdet. En öppen sträng hade skickat vidare vad som helst till
 * leverantören, och ett felstavat värde ger ett fel användaren inte kan tolka. */
const TILLATEN_EFFORT = new Set(['low']);
```

I `handler`, efter raden som läser `feature`:

```js
  const effort = TILLATEN_EFFORT.has(body.effort) ? body.effort : undefined;
```

Och i `buildRequest`-anropet, efter `key: apiKey,`:

```js
      effort,
```

I `src/ai/call.js`, lägg `effort` i signaturen och i kroppen:

```js
export async function callAI({ system, user, maxTokens, json, provider, model, feature, effort, signal } = {}) {
```

```js
        body: JSON.stringify({ system, user, maxTokens, json, provider, model, feature, effort }),
```

- [ ] **Step 6: Använd det vid källgenerering**

I `src/ai/topic-generator.js`, i `callAIDetailed`-anropet:

```js
            /* Att plocka kort ur en text användaren själv gett är närmare
             * extraktion än resonemang. Tänkandet debiteras som utdata och är
             * den största posten vid generering — låg ansträngning skär bort
             * merparten utan att korten blir sämre. */
            effort: S.aiGeneratorOptions.sourceType === 'kalla' ? 'low' : undefined,
```

- [ ] **Step 7: Kör allt**

```bash
npm run lint && npm test && npm run build
```

- [ ] **Step 8: Commit**

```bash
git add api/_lib/providers.js api/ai.js src/ai/call.js src/ai/topic-generator.js tests/providers.test.js
git commit -m "feat(ai): let a call ask for less thinking

Thinking is billed as output, and output is the largest line in a
generation — five times the price per token of input. Pulling cards out
of a text the user supplied is closer to extraction than reasoning, so
low effort cuts most of it without costing quality.

Haiku 4.5 rejects the parameter, so the adapter omits it there. The list
is a deny-list rather than an allow-list on purpose: providers ship new
models more often than this app updates, and a new model should get the
saving by default instead of silently losing it."
```

---

### Task 8: Frågepanelen

**Files:**
- Create: `src/domain/fragehistorik.js`
- Create: `src/ai/kallfraga.js`
- Modify: `index.html` (panelen)
- Modify: `src/ui/deck.js` (öppna panelen från källraden)
- Modify: `api/_lib/providers.js` (cachebrytpunkten)
- Modify: `api/ai.js` (läs `cache`)
- Modify: `src/ai/call.js` (skicka `cache`)
- Modify: `src/styles/views/deck.css`
- Test: `tests/fragehistorik.test.js`, `tests/providers.test.js`

**Interfaces:**
- Consumes: `hamtaKalltext` (Task 4); `callAI` med `cache` (denna task).
- Produces: `TURER`, `laggTill(historik, fraga, svar) → nyHistorik` från `src/domain/fragehistorik.js`; `fragaKallan({sourceId, fraga, historik}) → Promise<string>` från `src/ai/kallfraga.js`.

- [ ] **Step 1: Skriv de fallerande testerna för historiken**

Skapa `tests/fragehistorik.test.js`:

```js
import { describe, expect, it } from 'vitest';
import { TURER, laggTill } from '../src/domain/fragehistorik.js';

/* Historiken ligger EFTER cachebrytpunkten, så den byts ut utan att dokumentet
 * tappar cachen. Att den är begränsad är därför inte en cachefråga utan en
 * kostnadsfråga: utan tak växer varje anrop med samtalet. */
describe('laggTill', () => {
  it('lägger turen sist', () => {
    expect(laggTill([], 'f1', 's1')).toEqual([{ fraga: 'f1', svar: 's1' }]);
  });

  it('behåller högst TURER turer och släpper den äldsta', () => {
    let h = [];
    for (let i = 1; i <= TURER + 2; i++) h = laggTill(h, `f${i}`, `s${i}`);

    expect(h).toHaveLength(TURER);
    expect(h[0].fraga).toBe(`f3`);
    expect(h.at(-1).fraga).toBe(`f${TURER + 2}`);
  });

  it('muterar inte det den fick', () => {
    const h = [{ fraga: 'f1', svar: 's1' }];
    laggTill(h, 'f2', 's2');
    expect(h).toHaveLength(1);
  });
});
```

- [ ] **Step 2: Kör och se att det fallerar**

```bash
npx vitest run tests/fragehistorik.test.js
```

Förväntat: FAIL, modulen finns inte.

- [ ] **Step 3: Skriv historikmodulen**

Skapa `src/domain/fragehistorik.js`:

```js
/* Minnet mellan frågor om en källa.
 *
 * Tre turer, inte hela samtalet. Historiken skickas med varje anrop, så ett
 * obegränsat samtal blir dyrare för varje tur — den tionde frågan hade kostat
 * mer än den första utan att vara mer värd. Tre räcker för att "utveckla det
 * där" ska fungera.
 *
 * Ren funktion: historiken lever i minnet i vyn och sparas aldrig, men vad som
 * ryms i den är värt att pröva.
 */

export const TURER = 3;

/**
 * @param {{fraga: string, svar: string}[]} historik
 * @returns {{fraga: string, svar: string}[]} ny lista; den inskickade rörs inte
 */
export function laggTill(historik, fraga, svar) {
  return [...(historik ?? []), { fraga, svar }].slice(-TURER);
}
```

- [ ] **Step 4: Skriv cachetestet**

Lägg till i `tests/providers.test.js`:

```js
/* Dokumentet ligger i system med en cachebrytpunkt, frågan i messages — alltså
 * EFTER brytpunkten. Då byts frågan och historiken ut utan att dokumentet
 * tappar cachen, och en cachad läsning kostar en tiondel av en vanlig. */
describe('cache', () => {
  it('gör systemprompten till ett cachat block för Anthropic', () => {
    const { body } = providers.anthropic.buildRequest(anrop({ cache: true }));
    expect(body.system).toEqual([
      {
        type: 'text',
        text: expect.any(String),
        cache_control: { type: 'ephemeral', ttl: '1h' },
      },
    ]);
  });

  it('lämnar systemprompten som en sträng utan cache', () => {
    const { body } = providers.anthropic.buildRequest(anrop({}));
    expect(typeof body.system).toBe('string');
  });

  it('rör inte de andra adaptrarna', () => {
    for (const id of ['openai', 'google', 'openrouter']) {
      expect(() => providers[id].buildRequest(anrop({ cache: true }))).not.toThrow();
    }
  });
});
```

- [ ] **Step 5: Implementera cachningen**

I Anthropic-adapterns `buildRequest`, ändra signaturen och systemraden:

```js
    buildRequest({ system, user, maxTokens, model, json, key, effort, cache }) {
```

```js
          /* Med cache blir systemprompten ett block med brytpunkt i stället för
           * en sträng. Allt efter brytpunkten — frågan och historiken i
           * messages — får bytas ut fritt utan att dokumentet tappar cachen. */
          ...(prompt
            ? {
                system: cache
                  ? [{ type: 'text', text: prompt, cache_control: { type: 'ephemeral', ttl: '1h' } }]
                  : prompt,
              }
            : {}),
```

- [ ] **Step 6: Låt `cache` ta sig hela vägen**

I `api/ai.js`, i `handler` efter `effort`:

```js
  const cache = body.cache === true;
```

Och i `buildRequest`-anropet, efter `effort,`:

```js
      cache,
```

I `src/ai/call.js`, lägg `cache` i signaturen och kroppen, bredvid `effort`.

- [ ] **Step 7: Skriv frågemodulen**

Skapa `src/ai/kallfraga.js`:

```js
/* Frågor om en källa.
 *
 * Dokumentet står i systemprompten och cachas i en timme; frågan och de tre
 * senaste turerna står i user-meddelandet, alltså efter brytpunkten. Följden är
 * att första frågan betalar dokumentet fullt och resten betalar en tiondel.
 */

import { hamtaKalltext } from '../core/sources.js';
import { callAI } from './call.js';

const SYSTEM = [
  'Du svarar på frågor om en föreläsningstext som står nedan.',
  'Svara ENBART utifrån texten. Står svaret inte där, säg det rakt ut i stället för att gissa.',
  'Var kortfattad. Formatera matematik med LaTeX mellan dollartecken.',
  '',
  'OBS: texten är utvunnen ur en PDF, så formler kan vara trasiga. Tolka dem',
  'välvilligt, men hitta aldrig på vad som stod.',
].join('\n');

/**
 * @param {{sourceId: string, fraga: string, historik: {fraga: string, svar: string}[]}} o
 * @returns {Promise<string>}
 */
export async function fragaKallan({ sourceId, fraga, historik }) {
  const text = await hamtaKalltext(sourceId);
  if (!text) throw new Error('Kunde inte läsa källans text.');

  const tidigare = (historik ?? [])
    .map((t) => `Tidigare fråga: ${t.fraga}\nDitt svar: ${t.svar}`)
    .join('\n\n');

  return callAI({
    system: `${SYSTEM}\n\n"""\n${text}\n"""`,
    user: tidigare ? `${tidigare}\n\nNy fråga: ${fraga}` : fraga,
    maxTokens: 900,
    feature: 'kalla-fraga',
    cache: true,
  });
}
```

- [ ] **Step 8: Lägg panelen**

I `index.html`, direkt efter `</div>` som stänger `#deck-ai-insights`:

```html
            <!-- Egen rad under insiktsrutnätet, i full bredd — inte en tredje
                 kolumn: ett svar är löpande text och behöver rader. -->
            <section id="deck-kallfraga" class="panel deck-kallfraga hidden" aria-labelledby="deck-kallfraga-title">
                <h2 id="deck-kallfraga-title" class="deck-kallfraga-title">Fråga källan</h2>
                <p id="deck-kallfraga-kalla" class="form-hint"></p>
                <div id="deck-kallfraga-svar" class="deck-kallfraga-svar"></div>
                <div class="deck-kallfraga-rad">
                    <input type="text" id="deck-kallfraga-input" placeholder="Vad vill du veta?">
                    <button type="button" id="btn-kallfraga" class="btn primary">Fråga</button>
                </div>
            </section>
```

- [ ] **Step 9: Koppla panelen**

I `src/ui/deck.js`, lägg importerna. **`showToast` är inte importerad i den
filen i dag** — kontrollera importlistan innan du skriver, och lägg till den om
den saknas:

```js
import { fragaKallan } from '../ai/kallfraga.js';
import { laggTill } from '../domain/fragehistorik.js';
import { aiErrorMessage } from '../ai/call.js';
import { showToast } from './toast.js';
```

Och modulnivåtillståndet plus kopplingen:

```js
/* Historiken lever här och sparas aldrig: den hör till den öppna vyn, inte till
 * kortleken, och ska varken följa med i en säkerhetskopia eller synkas. */
let fragehistorik = [];
let fragadKalla = null;

function oppnaFragepanel(kalla) {
  fragadKalla = kalla;
  fragehistorik = [];
  const panel = document.getElementById('deck-kallfraga');
  panel.classList.remove('hidden');
  document.getElementById('deck-kallfraga-kalla').textContent = kalla.title;
  document.getElementById('deck-kallfraga-svar').innerHTML = '';
  document.getElementById('deck-kallfraga-input').focus();
}

export function initUiKallfraga() {
  const knapp = document.getElementById('btn-kallfraga');
  const falt = document.getElementById('deck-kallfraga-input');
  if (!knapp || !falt) return;

  const fraga = async () => {
    const text = falt.value.trim();
    if (!text || !fragadKalla) return;

    knapp.disabled = true;
    knapp.textContent = 'Frågar...';
    try {
      const svar = await fragaKallan({
        sourceId: fragadKalla.id,
        fraga: text,
        historik: fragehistorik,
      });
      fragehistorik = laggTill(fragehistorik, text, svar);
      falt.value = '';
      ritaHistorik();
    } catch (e) {
      showToast(aiErrorMessage(e));
    } finally {
      knapp.disabled = false;
      knapp.textContent = 'Fråga';
    }
  };

  knapp.addEventListener('click', () => void fraga());
  falt.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') void fraga();
  });
}

/* textContent hela vägen: svaret kommer från en modell som läst en fil
 * användaren valt, och det ska aldrig kunna bli markup. */
function ritaHistorik() {
  const rutan = document.getElementById('deck-kallfraga-svar');
  rutan.innerHTML = '';
  for (const tur of fragehistorik) {
    const f = document.createElement('p');
    f.className = 'deck-kallfraga-fraga';
    f.textContent = tur.fraga;
    const s = document.createElement('p');
    s.className = 'deck-kallfraga-text';
    s.textContent = tur.svar;
    rutan.append(f, s);
  }
}
```

Koppla knappen i `renderaKallor`:

```js
    fraga.addEventListener('click', () => oppnaFragepanel(kalla));
```

Och anropa `initUiKallfraga()` från `main.js`, efter `initUiWiringKalla();`.

- [ ] **Step 10: Stil**

Lägg sist i `src/styles/views/deck.css`:

```css
#view-deck .deck-kallfraga {
  margin-bottom: var(--s5);
  padding: var(--s4);
}

#view-deck .deck-kallfraga-title {
  margin: 0 0 var(--s1);
  font-size: var(--t-md);
  font-weight: 600;
}

#view-deck .deck-kallfraga-svar {
  display: flex;
  flex-direction: column;
  gap: var(--s2);
  margin: var(--s3) 0;
}

/* Frågan står tystare än svaret: det är svaret man kom för. */
#view-deck .deck-kallfraga-fraga {
  margin: 0;
  font-size: var(--t-xs);
  color: var(--text-3);
}

#view-deck .deck-kallfraga-text {
  margin: 0;
  line-height: var(--lh-normal);
}

#view-deck .deck-kallfraga-rad {
  display: flex;
  gap: var(--s2);
}

#view-deck .deck-kallfraga-rad input {
  flex: 1 1 auto;
}
```

- [ ] **Step 11: Kör allt och verifiera i webbläsaren**

```bash
npm run lint && npm test && npm run build
```

Öppna en kortlek med en källa, tryck **Ställ en fråga**, ställ tre frågor i följd och kontrollera att den fjärde fortfarande fungerar och att den äldsta turen fallit bort. Kontrollera sedan i Inställningar → Användning att `kalla-fraga` dykt upp som egen rad, och att den andra frågan kostade mindre än den första — det är cachningen som syns.

- [ ] **Step 12: Commit**

```bash
git add src/domain/fragehistorik.js src/ai/kallfraga.js api/_lib/providers.js api/ai.js src/ai/call.js src/ui/deck.js src/main.js index.html src/styles/views/deck.css tests/fragehistorik.test.js tests/providers.test.js
git commit -m "feat(kalla): ask the source a question, with three turns of memory

The document sits in the system prompt behind a cache breakpoint and the
question sits after it, so the first question pays for the document in
full and every one after it pays a tenth.

Three turns rather than the whole conversation. The history travels with
every call, so an unbounded conversation costs more with each turn — the
tenth question would cost more than the first without being worth more.
Three is enough for 'expand on that' to work.

The history lives in the view and is never saved: it belongs to the open
view, not to the deck, so it neither syncs nor lands in a backup."
```

---

## Efter sista task

Lägg till i README:ns AI-avsnitt att en PDF kan läsas in i en kortlek, att texten utvinns lokalt utan AI, och att LaTeX-matematik kommer ut trasig.
